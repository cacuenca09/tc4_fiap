global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "lstm-api"
    static_configs:
      - targets: ["lstm-api:8000"]
    metrics_path: /metrics
    scrape_interval: 30s
